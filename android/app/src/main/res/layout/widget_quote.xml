<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_quote_bg"
    android:orientation="vertical"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    android:paddingTop="12dp"
    android:paddingBottom="12dp">

    <TextView
        android:id="@+id/widget_kicker"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/widget_quote_kicker"
        android:textAllCaps="true"
        android:textColor="#C9A227"
        android:textSize="10sp"
        android:letterSpacing="0.12"
        android:fontFamily="sans-serif-medium" />

    <TextView
        android:id="@+id/widget_quote"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="6dp"
        android:ellipsize="end"
        android:fontFamily="serif"
        android:maxLines="6"
        android:text="«Бери лаваш и вали, психопат!»"
        android:textColor="#ECEAE4"
        android:textSize="16sp"
        android:textStyle="italic"
        android:lineSpacingExtra="2dp" />

    <TextView
        android:id="@+id/widget_speaker"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:text="Повар шавермы"
        android:textColor="#9A958C"
        android:textSize="12sp" />
</LinearLayout>
